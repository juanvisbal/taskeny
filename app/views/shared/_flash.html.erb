<div id="flash" class="mb-6">
  <% if flash.any? %>
    <% flash.each do |type, message| %>
      <div class="p-4 rounded-lg <%= type == 'notice' ? 'bg-green-100 text-green-800' : 'bg-red-100 text-red-800' %>">
        <%= message %>
      </div>
    <% end %>
  <% end %>
</div>
